interface:
  display_name: "IOC Analysis"
  short_description: "Google SecOps IOC search and triage"
  default_prompt: "Use $ioc-analysis to classify these IOCs and search them in the configured Google SecOps MCP server."
dependencies:
  tools:
    - type: "mcp"
      value: "google_secops"
      description: "Configured Google SecOps / Chronicle SIEM MCP server"
policy:
  allow_implicit_invocation: true
